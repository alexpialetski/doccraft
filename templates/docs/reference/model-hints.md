# Model hints registry

This file is **project-owned**. Doccraft ships it as a neutral starter; the JSON Schema for `doccraft.json` does **not** validate its structure or contents. Edit freely to describe your environment.

## Available models

| Model / tier | Role | Notes |
|--------------|------|-------|
| _(fill in)_ | _(e.g. default, review, codegen)_ | _(one line each)_ |
| _(fill in)_ | | |

## Phases and roles (optional)

Many stories benefit from **different** models at different times — e.g. a heavier or more careful model for OpenSpec / spec drafting, and a faster or cheaper model for straight implementation. Describe that here in **prose** or a small table (phase → model / tier).

**In stories:** record the outcome as plain markdown **at the end of the Notes section** (after other Notes content), in normal sentences.

## Decision rules

_(Optional.)_ How model choice combines with story context (`impact`, `urgency`, tags, body, `openspec`). Omit until you have a stable convention.

## Per-story mapping

| Story `id` | Model / phase guidance (summary) | Rationale |
|------------|-----------------------------------|-----------|
| | | |

---

Starter template introduced in doccraft OpenSpec change `2026-05-10-story-model-hints`. Future doccraft versions may refresh this file via `doccraft update` when you have not customised it.
